# 数据库配置（支持环境变量覆盖）
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/oa_system
spring.datasource.username=${DB_USER:oa_admin}
spring.datasource.password=${DB_PASS:oa_pass}

# Redis配置
spring.redis.host=${REDIS_HOST:localhost}
spring.redis.port=${REDIS_PORT:6379}